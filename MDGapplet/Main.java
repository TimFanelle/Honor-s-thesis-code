import java.util.ArrayList;

public class Main {
	public static void main(String[] arg0){
		Game gg = new Game();
		/*
		ArrayList<Integer> l = new ArrayList<>();
		for(int i = 1; i<=75; i++){
			l.add(i);
		}
		String order = "";
		int choice;
		int elf = 1;
		while(l.size() > 0){
			choice = choosy(l.size()-1);
			order  = order + ", " + l.get(choice);
			if(elf == 15){
				order = order + "\n";
				elf = 0;
			}
			l.remove(choice);
			elf++;
		}
		System.out.println(order);
*/
	}
	/*
	private static int choosy(int ll){
		return (int)(Math.random()*ll);
	}

	 */
}
